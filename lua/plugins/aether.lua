-- Aether disabled as startup theme (too muted / low-color for TS work).
-- Catppuccin mocha (lua/plugins/catppuccin.lua) is the active theme.
-- Aether is still available lazily via all-themes.lua for hot-swapping.
return {}
