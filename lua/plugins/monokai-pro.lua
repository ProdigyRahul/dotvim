-- Eager monokai-pro spec disabled: aether (lua/plugins/aether.lua) is the startup
-- theme, so loading+applying monokai-pro first was pure wasted startup work.
-- The plugin is still available lazily via all-themes.lua for hot-swapping.
return {}
